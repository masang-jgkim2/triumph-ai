# 로컬 클론 (repos.lock.json으로 SHA 추적)
/repos/

# 로컬 비밀/오버라이드 (프론트 Vite — Git 커밋 금지)
env
env.qa
env.production
.env.local
config/local.env

# 로그·PID
/logs/
*.pid

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
